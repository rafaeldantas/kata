apply plugin: 'java'
apply plugin: 'eclipse'
apply plugin: 'jacoco'

repositories {
    jcenter()
}


jar {
    manifest {
        attributes 'Main-Class': 'aws.fleet.Main'
    }
}

defaultTasks 'clean', 'test', 'jacoco', 'jar'

dependencies {
    testCompile 'junit:junit:4.12'
}